export const freshState=()=>({version:1,name:'보리',xp:0,day:1,food:65,love:75,energy:80,bond:20,daily:[],dailyReward:false,memories:[{day:1,text:'작은 발자국이 우리 집에 왔어요. 오늘부터 우리는 가족.'}]});
export const stageOf=xp=>xp>=250?2:xp>=100?1:0;
const clamp=(x,min,max,fallback)=>Number.isFinite(x)?Math.min(max,Math.max(min,Math.round(x))):fallback;
export function normalize(data){const s=freshState();if(!data||data.version!==1)return s;for(const k of ['food','love','energy','bond'])s[k]=clamp(data[k],0,100,s[k]);s.xp=clamp(data.xp,0,999999,0);s.day=clamp(data.day,1,999999,1);s.name=typeof data.name==='string'&&data.name.trim()?data.name.trim().slice(0,12):s.name;s.daily=Array.isArray(data.daily)?data.daily.filter(x=>['feed','pet','play','walk','rest'].includes(x)):[];s.dailyReward=data.dailyReward===true;s.memories=Array.isArray(data.memories)?data.memories.filter(m=>m&&typeof m.text==='string'&&Number.isFinite(m.day)).slice(0,60).map(m=>({day:clamp(m.day,1,999999,1),text:m.text.slice(0,250)})):s.memories;return s}
export function applyCare(state,action){const effects={feed:[10,28,4,0,3],pet:[6,0,18,0,8],play:[12,-5,20,-12,6],walk:[18,-8,18,-15,10],rest:[8,-3,5,35,4]};if(!effects[action])return state;const s={...state,daily:[...new Set([...state.daily,action])]};const [xp,...stats]=effects[action];s.xp+=xp;['food','love','energy','bond'].forEach((k,i)=>s[k]=Math.max(0,Math.min(100,s[k]+stats[i])));return s}
